export enum LinearGroupByOptions {
    PROJECT = 'project',
    PRIORITY = 'priority',
    STATUS = 'status',
    NONE = 'none',
}

export enum LinearDisplayPropertiesOptions {
    PRIORITY = 'priority',
    STATUS = 'status',
    KEY = 'key',
    LABELS = 'labels',
    UPDATED = 'updatedAt',
    CREATED = 'createdAt',
    PROJECT = 'project',
    CYCLE = 'cycle',
    ASSIGNEE = 'assignee',
    CLIP = 'clip',
}

export enum LinearSortingOptions {
    PRIORITY = 'priority',
    KEY = 'key',
    STATUS = 'status',
    UPDATED = 'updated',
    CREATED = 'created',
}

export enum LinearSortDirectionOptions {
    ASCENDING = 'ascending',
    DESCENDING = 'descending',
}
